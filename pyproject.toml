[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mytool"
version = "0.1.0"
description = "My first pip CLI tool"
requires-python = ">=3.8"

[project.scripts]
todo = "mytool.cli:main"
